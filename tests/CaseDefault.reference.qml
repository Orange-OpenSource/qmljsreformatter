import QtQuick 2.5

Item {
    a: switch (a) {
       case a:
           a()
       case a:
       case a():
       default:
           a()
       }

    onA: {
        switch (a) {
        case a:
            a();
        case a:
        case a():
        default:
            a();
        }
    }
}
